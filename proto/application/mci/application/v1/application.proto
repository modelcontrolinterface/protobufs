syntax = "proto3";

package mci.application.v1;

service ApplicationService {
  rpc Connect(stream ConnectRequest) returns (stream ConnectResponse);
}

message ConnectRequest {
  string client_id = 1;
}

message ConnectResponse {
  string message_id = 1;
}
